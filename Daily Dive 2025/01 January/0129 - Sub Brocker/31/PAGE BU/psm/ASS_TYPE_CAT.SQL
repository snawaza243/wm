
create or replace PROCEDURE PSM_AM_GET_ASCT_TYPE_CAT (
    p_cursor OUT SYS_REFCURSOR
) 
IS
BEGIN
    -- Open a cursor 
    OPEN p_cursor FOR
    SELECT CATEGORY AS CATEGORY_ID, UPPER(TRIM(NAME)) AS CATEGORY_NAME FROM AGENT_CATEGORY WHERE CATEGORY IS NOT NULL  ORDER BY NAME;
END;
