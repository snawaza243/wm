select 
VERIFICATION_FLAG,
REJECTION_STATUS, 
TRAN_TYPE,
PUNCHING_FLAG , common_id, tb_doc_upload.*
from tb_doc_upload  WHERE 
COMMON_ID = '31153678'
tran_type != 'AC';

COMMON_ID = '10000197';

SELECT * FROM psm_tv_dt;


    SELECT COMMON_ID
    FROM TB_DOC_UPLOAD 
    WHERE VERIFICATION_FLAG = '1' 
      AND REJECTION_STATUS = '0' 
      AND TRAN_TYPE != 'AC' 
      AND PUNCHING_FLAG = '0' 
      AND COMMON_ID IS NOT NULL AND GUEST_CD IS NOT NULL;
      
      
      
      
      SELECT  * FROM BAJAJ_VENUE_BOOKING;