CREATE TABLE DUEPAID_MappedData (
    FileType VARCHAR2(50),
    MaskedStr VARCHAR2(500),
    CreatedTime TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    ModifiedUser VARCHAR2(50),
    LoginUser VARCHAR2(50),
    ModifiedTime TIMESTAMP,
    Action NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY
);

INSERT INTO DUEPAID_MAPPEDDATA VALUES("DUE", "MaskedStr", "ModifiedUser", "LoginUser", "MODIFIEDTIME");